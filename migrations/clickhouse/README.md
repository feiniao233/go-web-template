# ClickHouse migrations

Place ClickHouse-specific `*.up.sql` and `*.down.sql` files here when the telemetry schema is defined. Keep one DDL statement per file because ClickHouse DDL migrations are not transactional.
